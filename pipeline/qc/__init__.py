# Waystation QC engine — profile-driven, tiered (BLOCKER/ISSUE/FYI) media QC.
# Execution order (per the delivery architecture): structural parsing first,
# deterministic signal checks second, the semantic AI layer last (in worker.py).
